{% extends 'front/base.html.twig' %}

{% block title %}Modifier l'événement{% endblock %}

{% block content %}
<style>
    .course-form-container { padding: 0; background: #f8f9fa; min-height: 100vh; padding: 2rem 0 4rem; }
    .form-header { display: flex; flex-direction: column; background: radial-gradient(circle at 10% 20%, rgb(0, 89, 169) 0%, rgb(0, 31, 63) 90%); padding: 3rem 2rem 8rem 2rem; color: white; margin: 0 auto; width: 95%; max-width: 1250px; border-radius: 20px; box-shadow: 0 10px 30px rgba(0, 51, 102, 0.15); }
    .header-content { text-align: center; max-width: 800px; margin: 0 auto; }
    .header-content .page-title { font-size: 2.5rem; color: #fff; margin-bottom: 0.8rem; font-weight: 700; }
    .page-subtitle { color: rgba(255,255,255,0.9); font-size: 1.1rem; }
    .btn-back-custom { display: inline-flex; align-items: center; gap: 8px; color: rgba(255,255,255,0.9); text-decoration: none; font-weight: 500; }
    .btn-back-custom:hover { color: #fff; }
    .form-card { background: #fff; border-radius: 20px; box-shadow: 0 25px 50px -12px rgba(0,0,0,0.15); padding: 4rem; width: 95%; max-width: 1250px; margin: -5rem auto 0 auto; position: relative; z-index: 10; }
    .form-card::after { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 6px; background: linear-gradient(90deg, #00c6ff, #0072ff); border-radius: 20px 20px 0 0; }
    .section-title { font-size: 1.2rem; color: #1a1a1a; font-weight: 600; display: flex; align-items: center; gap: 0.5rem; }
    .btn-submit { padding: 1rem 2rem; background: #0059A9; color: #fff; border: none; border-radius: 12px; font-weight: 600; cursor: pointer; display: inline-flex; align-items: center; gap: 8px; }
    .btn-secondary-custom { padding: 1rem 2rem; border: 1px solid #d2d6da; border-radius: 12px; color: #344767; text-decoration: none; font-weight: 600; }
    .btn-secondary-custom:hover { background: #f8f9fa; }
    .edit-container { display:grid; grid-template-columns: 1fr 360px; gap:28px; align-items:start; max-width:1100px; }
    .edit-card { background:#fff; border:1px solid var(--border-color); border-radius:12px; padding:22px; box-shadow:var(--shadow-sm); }
    .edit-card h2 { margin-top:0; font-size:1.2rem; color:#1a1a1a; font-weight:600; }

    .form-group { display:flex; flex-direction:column; gap:6px; margin-bottom:14px; }
    .form-group label { font-weight:600; color:var(--text-dark); font-size:0.95rem; }

    .form-control { width:100%; padding:10px 12px; border-radius:10px; border:1px solid var(--border-color); background:#fff; font-size:0.95rem; transition:0.15s; }
    .form-control:focus { outline:none; border-color:var(--primary-dark); box-shadow:0 0 0 4px rgba(0,51,102,0.06); }

    textarea.form-control { min-height:120px; resize:vertical; }

    /* Error states */
    .has-error .form-control { border-color:#ff4757; background:rgba(255,71,87,0.03); }
    .form-error-message { color:#ff4757; font-size:0.85rem; margin-top:6px; display:flex; gap:8px; align-items:center; }

    .form-actions { display:flex; gap:10px; margin-top:18px; }
    .btn-primary { padding:10px 14px; border-radius:10px; background:linear-gradient(135deg,var(--primary-dark),var(--primary-light)); color:#fff; border:none; }
    .btn-secondary { padding:10px 14px; border-radius:10px; border:1px solid var(--border-color); background:transparent; color:var(--primary-dark); }

    .event-preview { background:#fff; border:1px solid var(--border-color); border-radius:12px; padding:18px; box-shadow:var(--shadow-sm); position:sticky; top:20px; }
    .event-preview h3 { margin-top:0; font-size:1.1rem; color:#1a1a1a; font-weight:600; border-bottom:1px solid var(--border-color); padding-bottom:12px; }

    @media (max-width:900px) { .edit-container { grid-template-columns:1fr; } .event-preview { position:relative; top:auto; } }
</style>

<div class="course-form-container">
    <div class="form-header">
        <div style="margin-bottom: 20px;">
            <a href="{{ path('app_temps') }}" class="btn-back-custom">
                <i class="fas fa-arrow-left"></i> Retour au planning
            </a>
        </div>
        <div class="header-content">
            <h1 class="page-title">Modifier l'événement</h1>
            <p class="page-subtitle">Mise à jour pour : <strong>{{ event.title }}</strong></p>
        </div>
    </div>

    <div class="form-card">
        <div class="edit-container">
    <div class="edit-card">
        <h2 class="section-title" style="border:none; padding:0; margin-bottom:1.5rem;"><i class="fas fa-calendar-alt" style="color:#00c6ff;"></i> Détails</h2>

        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

        <div style="display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:12px;">
            <div class="form-group">
                {{ form_label(form.title, 'Event Title *') }}
                {{ form_widget(form.title, {'attr': {'placeholder': 'Enter event title', 'class': 'form-control'}}) }}
                {% for error in form.title.vars.errors %}
                    <div class="form-error-message">{{ error.message }}</div>
                {% endfor %}
            </div>

            <div class="form-group">
                {{ form_label(form.date, 'Date *') }}
                {{ form_widget(form.date, {'attr': {'type': 'date', 'class': 'form-control'}}) }}
                {% for error in form.date.vars.errors %}
                    <div class="form-error-message">{{ error.message }}</div>
                {% endfor %}
            </div>

            <div class="form-group">
                {{ form_label(form.duration, 'Duration (minutes)') }}
                {{ form_widget(form.duration, {'attr': {'type': 'number', 'class': 'form-control', 'min': 1, 'max': 1440}}) }}
                {% for error in form.duration.vars.errors %}
                    <div class="form-error-message">{{ error.message }}</div>
                {% endfor %}
            </div>

            <div class="form-group">
                {{ form_label(form.location, 'Location') }}
                {{ form_widget(form.location, {'attr': {'placeholder': 'Event location', 'class': 'form-control'}}) }}
                {% for error in form.location.vars.errors %}
                    <div class="form-error-message">{{ error.message }}</div>
                {% endfor %}
            </div>

            <div class="form-group">
                {{ form_label(form.status, 'Status *') }}
                {{ form_widget(form.status, {'attr': {'class': 'form-control'}}) }}
                {% for error in form.status.vars.errors %}
                    <div class="form-error-message">{{ error.message }}</div>
                {% endfor %}
            </div>

            <div class="form-group">
                {{ form_label(form.priority, 'Priority *') }}
                {{ form_widget(form.priority, {'attr': {'class': 'form-control'}}) }}
                {% for error in form.priority.vars.errors %}
                    <div class="form-error-message">{{ error.message }}</div>
                {% endfor %}
            </div>

            <div class="form-group">
                {{ form_label(form.difficulty, 'Difficulty Level (1-5)') }}
                {{ form_widget(form.difficulty, {'attr': {'type': 'number', 'min': 1, 'max': 5, 'class': 'form-control'}}) }}
                {% for error in form.difficulty.vars.errors %}
                    <div class="form-error-message">{{ error.message }}</div>
                {% endfor %}
            </div>

            <div class="form-group" style="grid-column:1 / -1;">
                {{ form_label(form.description, 'Description') }}
                {{ form_widget(form.description, {'attr': {'rows': 4, 'class': 'form-control'}}) }}
                {% for error in form.description.vars.errors %}
                    <div class="form-error-message">{{ error.message }}</div>
                {% endfor %}
            </div>

            <div class="form-group" style="grid-column:1 / -1;">
                {{ form_label(form.type, 'Type') }}
                {{ form_widget(form.type, {'attr': {'class': 'form-control', 'placeholder': 'e.g., Exam, Project'}}) }}
                {% for error in form.type.vars.errors %}
                    <div class="form-error-message">{{ error.message }}</div>
                {% endfor %}
            </div>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn-submit"><i class="fas fa-save"></i> Enregistrer</button>
            <a href="{{ path('app_temps') }}" class="btn-secondary-custom"><i class="fas fa-times"></i> Annuler</a>
        </div>

        {{ form_end(form) }}
    </div>

    <div class="event-preview">
        <h3>Aperçu</h3>
        <div style="margin-bottom:12px;"><div style="font-size:0.85rem;color:#6c757d;">Titre</div><div style="font-weight:600;">{{ event.title ?? '—' }}</div></div>
        <div style="margin-bottom:12px;"><div style="font-size:0.85rem;color:#6c757d;">Date</div><div>{{ event.date ? event.date|date('d/m/Y') : '—' }}</div></div>
        <div style="margin-bottom:12px;"><div style="font-size:0.85rem;color:#6c757d;">Priorité</div><div>{{ event.priority ?? '—' }}</div></div>
        <div style="margin-bottom:12px;"><div style="font-size:0.85rem;color:#6c757d;">Statut</div><div>{{ event.status ?? '—' }}</div></div>
        <div style="margin-bottom:12px;"><div style="font-size:0.85rem;color:#6c757d;">Durée</div><div>{{ event.duration ?? '—' }} min</div></div>
        <div style="margin-bottom:12px;"><div style="font-size:0.85rem;color:#6c757d;">Lieu</div><div>{{ event.location ?? '—' }}</div></div>
        {% if event.description %}<div><div style="font-size:0.85rem;color:#6c757d;">Description</div><div>{{ event.description }}</div></div>{% endif %}
    </div>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function(){
    var d = new Date();
    var iso = d.getFullYear() + '-' + String(d.getMonth()+1).padStart(2,'0') + '-' + String(d.getDate()).padStart(2,'0');
    document.querySelectorAll('input[type="date"]').forEach(function(i){ i.setAttribute('min', iso); });
});
</script>
{% endblock %}
